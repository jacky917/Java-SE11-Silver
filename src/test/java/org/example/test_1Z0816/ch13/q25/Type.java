package org.example.test_1Z0816.ch13.q25;

import java.io.Serializable;

public enum Type implements Serializable {
    A(1), B(1),C(1);

    private final static int val = 0;

    private Type(int i) {
    }
}
